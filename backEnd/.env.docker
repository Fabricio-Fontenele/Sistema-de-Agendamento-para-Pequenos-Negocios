# String de conexão com o banco MySQL rodando no container "db"
DATABASE_URL="mysql://root:admin@db:3306/agendamento"

# Segredo para geração/verificação do JWT (mantenha forte em produção)
JWT_SECRET="SeuSegredinho"

# Ambiente de execução
NODE_ENV="development"

# Configurações para envio de e-mail (ajuste conforme seu provedor)
MAIL_HOST="smtp.gmail.com"
MAIL_PORT=587
MAIL_USER="seu-email@gmail.com"
MAIL_PASS="sua-senha-ou-app-password"

# URL do frontend rodando no container frontend (ajuste se necessário)
FRONTEND_URL="http://frontend:3000"